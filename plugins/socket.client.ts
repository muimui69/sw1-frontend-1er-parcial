// import { io, Socket } from 'socket.io-client';

export default defineNuxtPlugin((nuxtApp) => {
    // const socket: Socket = io(process.env.API_SOCKET || 'http://localhost:4000', {
    //     transports: ['websocket', 'polling'],
    // });

    // nuxtApp.provide('socket', socket);
});
